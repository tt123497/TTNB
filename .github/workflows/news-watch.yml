name: News Watch (1min)

on:
  schedule:
    # 周末全天每5分钟
    - cron: '*/5 * * * 6-7'
    # 工作日非交易时段: UTC 0-0:55(盘前CST 8-8:55) + UTC 8-23(盘后CST 16-次日8:00)
    # 交易时段(CST 9:00-15:55 = UTC 1-7)由 market-update 覆盖, 不重复跑
    - cron: '*/5 0,8-23 * * 1-5'
  workflow_dispatch:

jobs:
  fetch:
    runs-on: china
    defaults:
      run:
        shell: bash
    env:
      PYTHONUTF8: '1'
      LANG: zh_CN.UTF-8
    permissions:
      contents: write
    steps:
      - name: Checkout (via gh CLI)
        run: |
          rm -rf ./* .[!.]* ..?* 2>/dev/null || true
          gh repo clone tt123497/TTNB .

      - name: Fetch news only (4 data sources, HTTP only)
        run: python news_fetch_once.py

      - name: Commit and push
        run: |
          git config user.name "News Bot"
          git config user.email "news@sentinel.local"
          git add data.json
          git diff --staged --quiet && exit 0
          for i in 1 2 3; do
            git commit -m "news $(date '+%H:%M')"
            git pull --rebase origin main && git push origin main && exit 0
            git rebase --abort 2>/dev/null; git reset --soft HEAD~1 2>/dev/null
            sleep 3
          done
