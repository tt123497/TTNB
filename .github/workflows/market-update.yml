name: Live Market Update

on:
  push:
    branches:
      - main
  schedule:
    # CST 9:30-15:00 every 5min (UTC 1:30-7:00), split per-hour (range syntax unreliable on GitHub)
    - cron: '*/5 1 * * 1-5'
    - cron: '*/5 2 * * 1-5'
    - cron: '*/5 3 * * 1-5'
    - cron: '*/5 4 * * 1-5'
    - cron: '*/5 5 * * 1-5'
    - cron: '*/5 6 * * 1-5'
    - cron: '*/5 7 * * 1-5'
    - cron: '0 2 * * 6,0'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Fetch data
        run: python .github/scripts/fetch_data.py
      - name: Fetch news
        run: python .github/scripts/fetch_news.py
      - name: Discover sectors
        run: python .github/scripts/discover_sectors.py
      - name: Score sectors
        run: python .github/scripts/score_sectors.py
      - name: Build events + layout
        run: python .github/scripts/fetch_events.py
      - name: Commit and push
        run: |
          git config user.name "Sentinel Bot"
          git config user.email "bot@sentinel.local"
          git add data.json
          git diff --staged --quiet || (git commit -m "Update $(TZ='Asia/Shanghai' date '+%H:%M')" && git push)
